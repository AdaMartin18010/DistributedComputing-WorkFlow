# 边缘计算架构

## 概述

边缘计算是一种分布式计算范式，将计算、存储和网络资源从中心云下沉到靠近数据源的网络边缘，以降低延迟、减少带宽消耗，并满足实时性和隐私保护需求。

## 架构层次

```mermaid
graph TB
    subgraph Cloud["中心云 (Cloud)"]
        CloudApp["云端应用<br/>大数据分析/ML训练"]
        CloudDB[(云存储)]
        CloudMgmt["统一管理平台"]
    end
    
    subgraph EdgeCloud["边缘云 (Edge Cloud)"]
        EdgeGateway["边缘网关<br/>聚合/预处理"]
        EdgeApp["边缘应用<br/>实时分析"]
        EdgeDB[(边缘存储)]
    end
    
    subgraph EdgeDevice["边缘设备 (Edge Device)"]
        Device1["工业设备<br/>PLC/传感器"]
        Device2["智能终端<br/>摄像头/AGV"]
        Device3["IoT设备<br/>可穿戴设备"]
    end
    
    Device1 -->|MQTT/Modbus| EdgeGateway
    Device2 -->|HTTP/gRPC| EdgeGateway
    Device3 -->|CoAP| EdgeGateway
    
    EdgeGateway --> EdgeApp
    EdgeApp --> EdgeDB
    EdgeGateway -.->|管控通道| CloudMgmt
    EdgeApp -.->|同步/上报| CloudApp
```

## 边缘云协同

### 协同架构

```mermaid
graph LR
    subgraph Edge["边缘层"]
        E_Runtime["边缘运行时<br/>KubeEdge/EdgeX"]
        E_App1["视频分析"]
        E_App2["设备控制"]
        E_Cache["本地缓存"]
    end
    
    subgraph Network["网络层"]
        SDWAN["SD-WAN<br/>智能路由"]
        5G["5G MEC<br/>多接入边缘"]
    end
    
    subgraph Cloud["云层"]
        C_Mgmt["边缘管理器"]
        C_AI["AI模型训练"]
        C_Store["长期存储"]
    end
    
    E_Runtime --> SDWAN --> C_Mgmt
    E_Runtime --> 5G --> C_Mgmt
    C_AI -.->|模型下发| E_Runtime
    E_Runtime -.->|数据上报| C_Store
```

## 关键技术栈

### KubeEdge架构

```mermaid
graph TB
    subgraph CloudSide["云端"]
        CloudCore["CloudCore"]
        Controller["EdgeController"]
        DeviceController["DeviceController"]
        SyncController["SyncController"]
    end
    
    subgraph EdgeSide["边缘端"]
        EdgeCore["EdgeCore"]
        MetaManager["MetaManager"]
        EdgeHub["EdgeHub"]
        EventBus["EventBus<br/>MQTT Broker"]
        DeviceTwin["DeviceTwin"]
        ServiceBus["ServiceBus"]
    end
    
    CloudCore <-->|WebSocket/QUIC| EdgeHub
    EdgeHub --> MetaManager
    MetaManager --> EventBus
    EventBus --> DeviceTwin
    DeviceController -.->|CRD| DeviceTwin
```

### 部署配置示例

```yaml
# KubeEdge边缘节点配置
apiVersion: v1
kind: ConfigMap
metadata:
  name: edgecore-config
  namespace: kubeedge
data:
  edgecore.yaml: |
    modules:
      edged:
        nodeIP: 192.168.1.100
        podSandboxImage: kubeedge/pause:3.1
        runtimeType: docker
        remoteRuntimeEndpoint: unix:///var/run/dockershim.sock
      edgehub:
        websocket:
          server: cloudcore.kubeedge.svc.cluster.local:10000
        quic:
          server: cloudcore.kubeedge.svc.cluster.local:10001
        token: <edge-node-token>
        httpserver: https://cloudcore.kubeedge.svc.cluster.local:10002
      eventbus:
        mqttMode: 2  # 0: internal, 1: external, 2: both
        mqttServer:
          internal:
            server: tcp://127.0.0.1:1883
          external:
            server: tcp://mqtt-broker:1883
            username: edge
            password: password
```

## 边缘智能

### AI推理部署

```mermaid
graph TB
    subgraph ModelMgmt["模型管理"]
        Train["云端训练"]
        Optimize["模型优化<br/>量化/剪枝"]
        Registry["模型仓库"]
    end
    
    subgraph EdgeInference["边缘推理"]
        Runtime["推理运行时<br/>TensorRT/ONNX"]
        App1["缺陷检测"]
        App2["行为识别"]
        App3["预测性维护"]
    end
    
    Train --> Optimize --> Registry
    Registry -.->|OTA更新| Runtime
    Runtime --> App1
    Runtime --> App2
    Runtime --> App3
```

```yaml
# 边缘AI推理应用部署
apiVersion: apps/v1
kind: Deployment
metadata:
  name: edge-inference-app
  namespace: edge
spec:
  replicas: 1
  selector:
    matchLabels:
      app: edge-inference
  template:
    metadata:
      labels:
        app: edge-inference
    spec:
      nodeSelector:
        node-type: edge
      tolerations:
      - key: "edge"
        operator: "Equal"
        value: "true"
        effect: "NoSchedule"
      containers:
      - name: inference
        image: edge-ai/inference:v1.0
        resources:
          limits:
            nvidia.com/gpu: 1  # GPU加速
            memory: "4Gi"
          requests:
            memory: "2Gi"
        volumeMounts:
        - name: model-volume
          mountPath: /models
        - name: video-input
          mountPath: /dev/video0
        env:
        - name: MODEL_PATH
          value: /models/yolov5s.onnx
        - name: INFERENCE_BATCH_SIZE
          value: "1"
        - name: TARGET_FPS
          value: "30"
      volumes:
      - name: model-volume
        hostPath:
          path: /opt/edge/models
      - name: video-input
        hostPath:
          path: /dev/video0
```

## 场景应用

| 场景 | 边缘能力 | 云端能力 | 协同方式 |
|-----|---------|---------|---------|
| 智能制造 | 实时质检、设备控制 | 生产优化、模型训练 | 模型下发、数据汇聚 |
| 智慧城市 | 交通监控、应急响应 | 城市规划、大数据分析 | 事件上报、策略下发 |
| 车联网 | 自动驾驶、车路协同 | 高精地图、模型更新 | V2X通信、OTA升级 |
| 智慧零售 | 客流分析、智能结算 | 供应链优化、用户画像 | 数据汇总、促销推送 |

## 挑战与解决方案

```mermaid
graph TB
    subgraph Challenges["边缘计算挑战"]
        C1["资源受限"]
        C2["网络不稳定"]
        C3["安全威胁"]
        C4["管理复杂"]
    end
    
    subgraph Solutions["解决方案"]
        S1["轻量级运行时<br/>WebAssembly/容器"]
        S2["离线自治<br/>本地缓存/断点续传"]
        S3["零信任安全<br/>mTLS/设备认证"]
        S4["云边协同管理<br/>GitOps/边缘编排"]
    end
    
    C1 --> S1
    C2 --> S2
    C3 --> S3
    C4 --> S4
```

## 总结

边缘计算通过将算力下沉，解决了中心化云计算在延迟、带宽和隐私方面的局限。随着5G、AI和云原生技术的发展，边缘计算与中心云的协同将成为未来计算架构的主流形态，推动工业互联网、智能交通等领域的数字化转型。
