# 零信任架构 - ZTA原则与实现

## 概述

零信任架构（Zero Trust Architecture, ZTA）是一种"从不信任，始终验证"的安全理念。传统边界安全模型假设内网可信，而零信任认为攻击可能来自任何地方，要求对每个访问请求进行严格的身份验证和授权。

## 零信任核心原则

```mermaid
graph TB
    subgraph 零信任三大原则
    A[永不信任<br/>Never Trust]
    B[始终验证<br/>Always Verify]
    C[假设泄露<br/>Assume Breach]
    end

    A --> A1[内网≠可信]
    A --> A2[外网≠不可信]

    B --> B1[身份验证]
    B --> B2[设备验证]
    B --> B3[行为分析]

    C --> C1[最小权限]
    C --> C2[持续监控]
    C --> C3[分段隔离]
```

## 传统模型 vs 零信任

```mermaid
graph TB
    subgraph 传统边界模型
    T1[互联网] --> T2[防火墙]
    T2 --> T3[DMZ]
    T3 --> T4[内网: 完全信任]
    T4 --> T5[应用A]
    T4 --> T6[应用B]
    T4 --> T7[数据库]

    style T4 fill:#90EE90
    end

    subgraph 零信任模型
    Z1[互联网] --> Z2[身份感知代理]
    Z3[内网] --> Z2
    Z2 --> Z4[策略决策点]
    Z4 -->|允许| Z5[应用A]
    Z4 -->|拒绝| Z6[应用B]
    Z5 --> Z7[数据库]

    Z4 -.mTLS.-> Z5
    Z5 -.mTLS.-> Z7

    style Z2 fill:#FFB6C1
    style Z4 fill:#FFB6C1
    end
```

## 零信任架构组件

### NIST零信任架构

```mermaid
graph TB
    subgraph 控制平面
    PE[策略引擎<br/>Policy Engine]
    PA[策略管理器<br/>Policy Administrator]
    end

    subgraph 数据平面
    PEP[策略执行点<br/>Policy Enforcement Point]
    end

    subgraph 信号源
    CDM[CDM系统<br/>设备状态]
    IDP[身份提供者<br/>用户身份]
    TI[威胁情报<br/>风险评分]
    SIEM[SIEM<br/>行为分析]
    end

    PE -->|决策| PA
    PA -->|配置| PEP

    CDM -->|设备信任| PE
    IDP -->|身份断言| PE
    TI -->|威胁数据| PE
    SIEM -->|异常检测| PE

    PEP -.访问请求.-> PE
```

### 核心组件详解

| 组件 | 功能 | 示例产品 |
|-----|------|---------|
| 策略引擎 | 评估访问请求，做出授权决策 | OPA, AWS IAM |
| 策略管理器 | 执行策略决策，配置执行点 | Istio, Envoy |
| 策略执行点 | 拦截流量，强制执行策略 | API网关, Sidecar |
| 身份目录 | 存储和管理身份信息 | Azure AD, Okta |
| 设备目录 | 设备状态和健康度 | CrowdStrike, Intune |

## 实现架构

### 企业零信任部署

```mermaid
graph TB
    subgraph 用户层
    U1[员工] --> U2[设备]
    U3[合作伙伴] --> U4[BYOD]
    end

    subgraph 接入层
    A1[身份感知代理<br/>IAP]
    A2[设备认证]
    A3[条件访问]
    end

    subgraph 策略层
    P1[统一身份<br/>IdP]
    P2[策略决策<br/>OPA]
    P3[风险引擎]
    end

    subgraph 资源层
    R1[SaaS应用]
    R2[私有应用]
    R3[API服务]
    R4[数据库]
    end

    U2 --> A1
    U4 --> A1
    A1 --> A2 --> A3
    A3 --> P1
    P1 --> P2
    P3 --> P2
    P2 -->|允许| R1
    P2 -->|允许| R2
    P2 -->|允许| R3
    R2 --> R4
```

### 微服务零信任实现

```yaml
# Istio + OPA 零信任配置

# 1. 身份验证策略
apiVersion: security.istio.io/v1beta1
kind: RequestAuthentication
metadata:
  name: jwt-auth
  namespace: production
spec:
  selector:
    matchLabels:
      app: order-service
  jwtRules:
  - issuer: "https://auth.example.com"
    jwksUri: "https://auth.example.com/.well-known/jwks.json"
    audiences:
    - "order-service"
    forwardOriginalToken: true
---
# 2. 授权策略
apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: order-authz
  namespace: production
spec:
  selector:
    matchLabels:
      app: order-service
  action: ALLOW
  rules:
  - from:
    - source:
        requestPrincipals: ["*"]
    when:
    - key: request.auth.claims[role]
      values: ["admin", "order-manager"]
  - to:
    - operation:
        methods: ["GET"]
        paths: ["/api/v1/orders/*"]
    when:
    - key: request.auth.claims[role]
      values: ["user"]
---
# 3. OPA策略
apiVersion: v1
kind: ConfigMap
metadata:
  name: opa-policy
  namespace: production
data:
  policy.rego: |
    package istio.authz

    import input.attributes.request.http as http_request
    import input.attributes.source as source

    default allow = false

    # 基于设备健康度决策
    allow {
        http_request.method == "POST"
        input.parsed_path[0] == "api"
        data.device_health[device_id].score > 80
    }

    # 基于地理位置决策
    allow {
        http_request.method == "GET"
        geoip_country(source.address.socketAddress.address) in ["CN", "US", "SG"]
    }

    # 基于异常行为决策
    allow {
        not data.threat_intel[source.address.socketAddress.address].malicious
    }
```

## 身份与设备验证

### 条件访问策略

```mermaid
flowchart TD
    A[访问请求] --> B{身份验证}
    B -->|失败| C[拒绝访问]
    B -->|通过| D{设备合规检查}
    D -->|不合规| E[限制访问/Remediation]
    D -->|合规| F{风险评估}
    F -->|高风险| G[MFA挑战]
    F -->|中风险| H[记录审计日志]
    F -->|低风险| I[允许访问]
    G -->|通过| I
    G -->|失败| C
```

### 设备信任评分

```yaml
# 设备健康度评估
 device_health_scoring:
  dimensions:
    - name: endpoint_security
      weight: 30%
      checks:
        - antivirus_installed: 10_points
        - firewall_enabled: 10_points
        - disk_encryption: 10_points

    - name: patch_compliance
      weight: 25%
      checks:
        - os_up_to_date: 15_points
        - critical_patches_installed: 10_points

    - name: behavioral_risk
      weight: 25%
      checks:
        - no_suspicious_processes: 10_points
        - no_known_bad_ips: 10_points
        - normal_login_patterns: 5_points

    - name: certificate_validity
      weight: 20%
      checks:
        - valid_device_cert: 10_points
        - cert_not_expired: 10_points

  trust_levels:
    high: score >= 80
    medium: 50 <= score < 80
    low: score < 50
```

## 微分段实现

### 网络微分段

```mermaid
graph TB
    subgraph 传统网络
    T1[VLAN 10] --> T2[所有应用]
    end

    subgraph 微分段网络
    M1[前端服务<br/>标签: tier=frontend] --> M2[API网关<br/>只允许: frontend]
    M2 --> M3[订单服务<br/>只允许: gateway]
    M2 --> M4[用户服务<br/>只允许: gateway]
    M3 --> M5[订单数据库<br/>只允许: order-service]
    M4 --> M6[用户数据库<br/>只允许: user-service]

    M7[支付服务<br/>只允许: order-service] --> M8[支付网关<br/>DMZ]
    end

    style M2 fill:#FFB6C1
    style M5 fill:#90EE90
    style M6 fill:#90EE90
    style M8 fill:#FFD700
```

### 应用层分段策略

```yaml
# Calico网络策略实现微分段
apiVersion: projectcalico.org/v3
kind: NetworkPolicy
metadata:
  name: payment-service-policy
  namespace: production
spec:
  selector: app == 'payment-service'
  types:
  - Ingress
  - Egress
  ingress:
  # 只允许订单服务访问支付服务的/process端点
  - action: Allow
    protocol: TCP
    source:
      selector: app == 'order-service'
    destination:
      ports:
      - 8080
      selector: app == 'payment-service'
    http:
      methods: ["POST"]
      paths:
      - exact: '/api/v1/payments/process'

  # 拒绝其他所有入站
  - action: Deny

  egress:
  # 只允许访问支付网关
  - action: Allow
    protocol: TCP
    destination:
      selector: app == 'payment-gateway'
      namespaceSelector: name == 'external'
      ports:
      - 443

  # 允许DNS查询
  - action: Allow
    protocol: UDP
    destination:
      ports:
      - 53
```

## 持续监控与响应

```mermaid
graph LR
    A[流量日志] --> B[UEBA引擎]
    C[端点遥测] --> B
    D[身份事件] --> B

    B --> E{异常检测}
    E -->|正常| F[允许继续]
    E -->|可疑| G[增强验证]
    E -->|恶意| H[自动阻断]

    G --> I[自适应响应]
    H --> I
    I --> J[策略更新]
    J --> K[策略下发]
```

---

*文档版本: v1.0 | 最后更新: 2026-04-03*
